name: textformfield_datetime_range_picker
description: A customizable date and time range picker widget for Flutter applications.
version: 0.0.2
homepage: https://github.com/adampermana/textformfield_datetime_range_picker.git
repository: https://github.com/adampermana/textformfield_datetime_range_picker.git
license: MIT

environment:
  sdk: '>=3.1.0 <4.0.0'
  flutter: '>=3.1.0'

dependencies:
  flutter:
    sdk: flutter
  flutter_web_plugins:
    sdk: flutter
  intl: ">=0.19.0 <0.21.0"
  plugin_platform_interface: ^2.0.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

# For information on the generic Dart part of this file, see the
# following page: https://dart.dev/tools/pub/pubspec

# The following section is specific to Flutter packages.
flutter:
  # This section identifies this Flutter project as a plugin project.
  # The 'pluginClass' specifies the class (in Java, Kotlin, Swift, Objective-C, etc.)
  # which should be registered in the plugin registry. This is required for
  # using method channels.
  # The Android 'package' specifies package in which the registered class is.
  # This is required for using method channels on Android.
  # The 'ffiPlugin' specifies that native code should be built and bundled.
  # This is required for using `dart:ffi`.
  # All these are used by the tooling to maintain consistency when
  # adding or updating assets for this project.
  plugin:
    platforms:
      android:
        package: com.example.textformfield_datetime_range_picker
        pluginClass: TextformfieldDatetimeRangePickerPlugin
      ios:
        pluginClass: TextformfieldDatetimeRangePickerPlugin
      web:
        pluginClass: TextformfieldDatetimeRangePickerWeb
        fileName: textformfield_datetime_range_picker_web.dart